services:
  slag-ai:
    build:
      context: .
    container_name: slag-ai
    ports:
      - "127.0.0.1:18081:8000"
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
      - ./config.yaml:/app/config.yaml:ro
